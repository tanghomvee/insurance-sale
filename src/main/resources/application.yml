server:
  port: 80

spring:
  mvc:
    static-path-pattern: '/static/**'
    view:
#      prefix: '/static'
      suffix: '.html'
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://49.4.114.69:3306/ins_crm?autoReconnect=true&useUnicode=true&characterEncoding=UTF8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 'root'


logging:
  level:
    root: info

